# ``Parser_Backtrack_Primitives``

@Metadata {
    @DisplayName("Parser Backtrack Primitives")
    @TitleHeading("Swift Primitives")
}

Effect-based non-deterministic-choice backtracking for parsers — `Parser.Backtrack`, an `Effect.Protocol` for exploring alternatives via multi-shot continuations. The seed member of the effectful-parsing domain.

## Topics
